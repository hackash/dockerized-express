/* exports configuration object based on environment */

if (process.env.NODE_ENV === 'production') {
    module.exports = require('./prod');
    return;
}

try {
    module.exports = Object.assign(require('./prod'), require('./dev'));
} catch (e) {
    module.exports = require('./prod');
}